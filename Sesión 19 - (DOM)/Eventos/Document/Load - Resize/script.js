window.addEventListener('load', ()=>{
    console.log('Página esta 100% Cargada.');
});

window.addEventListener('resize', (event)=>{
    console.log('La pantalla esta cambiando de tamaño', event);
});